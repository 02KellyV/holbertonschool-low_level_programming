#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
* main - Entry point.
* Return: 0 (success).
*/

int main(void)
{
	int n = 48;
	int m = 48;


	while (n <= 99)
	{
		while (m <= 99)
		{
			putchar(n);
			putchar(m);
			putchar(',');
		}m++;

		if (n == 99 && m == )
		{
			
		}
	}n++;

	putchar('\n');

	return (0);
}

